{
  "name": "bicthub",
  "version": "1.1.0",
  "description": "This is a note sharing patform for students",
  "license": "ISC",
  "author": "JAJA",
  "type": "commonjs",
  "scripts": {
    "start": "parcel index.html",
    "build": "parcel build index.html --no-optimize --no-scope-hoist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "parcel": "^2.15.4"
  }
}
